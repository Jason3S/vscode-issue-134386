/**
 * Sample class
 */
export class MyClass {
    // Type the text in the comment below. Pause at the `>`
    // private known = new Map<string, string>();
    private resolvedWords = new Map<string, string>();
    constructor(readonly words: string[]) {}
}
